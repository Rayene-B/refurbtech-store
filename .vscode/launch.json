{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open Main Screen",
            "file": "${workspaceFolder}/mainscreen.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
